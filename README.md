# MERN Application(assignment). CRUD deployments

## Folder Descript:

### In the Root folder type:
    ```
      npm install
    ```

### client: React UI code is there. Move here and type:
    ```
      npm install
    ```

### To run the server and client both enter:
    ```
      npm run dev
    ```
